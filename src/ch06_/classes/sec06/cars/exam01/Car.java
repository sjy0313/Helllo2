package ch06_.classes.sec06.cars.exam01;

public class Car {
	// p172 필드 초기값
	//필드(객체의 데이터를 저장하는 역할) 선언 : 맴버변수, 속성(사용자 정의 타입) 
	// 초기값을 지정하지 않으면 타입에 따른 초기값이 지정
	String model;
	boolean start;
	int speed;
}